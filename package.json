{
  "name": "terrors",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "make:migration": "bun features/scripts/generate-migration-files.ts",
    "migrate:dev": "bun features/scripts/migrate-dev.ts",
    "migrate:prod": "bun features/scripts/migrate-prod.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
